-- =============================================================================
--  server/src/models/sql/indexes.sql
-- =============================================================================
--  All CREATE INDEX / CREATE UNIQUE INDEX statements for the public schema.
--  Auto-generated by `scripts/dump-source-schema.ts`.
--
--  Extracted inventory (Phase 3 audit, count: ~60 indexes):
--    - Primary keys are emitted in tables.sql (as PRIMARY KEY constraints).
--    - This file holds the remaining B-tree, GIN, and partial indexes:
--        * activity_logs: reg_date, user_date, strava_unique, start_date partial
--        * challenges:    slug, category, type, tags(gin), start_at, created_at
--        * clubs:         slug, priority+is_public+status composite, tags(gin)
--        * blog_posts:    published, status
--        * user_notifications: user+read composite, created_at
--        * orders:        user_id, challenge_id, payment_status, created_at
--        * registrations: user_id, challenge_id, status
--        * ...and more per validate-schema output
--
--  DO NOT hand-edit — regenerate with `npm run schema:dump`.
-- =============================================================================
