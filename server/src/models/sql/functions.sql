-- =============================================================================
--  server/src/models/sql/functions.sql
-- =============================================================================
--  All PL/pgSQL and SQL functions from the public schema. Auto-generated by
--  `scripts/dump-source-schema.ts` using `pg_get_functiondef(oid)`.
--
--  Extracted inventory (40+ functions), grouped:
--
--  RBAC / auth helpers
--    has_role, is_admin, is_super_admin, get_user_roles, is_club_member
--
--  User lifecycle
--    handle_new_user            (trigger: auth.users → profiles/user_roles seed)
--
--  Challenge lifecycle
--    active_registration, register_for_challenge, cancel_active_registration,
--    expire_registrations, challenge_progress,
--    challenge_progress_by_registration, challenge_leaderboard,
--    global_leaderboard, hall_of_fame, list_public_clubs,
--    get_public_club_by_slug, list_club_members
--
--  Activity ingestion
--    _activity_type_matches_mode, _registration_logged_km,
--    ingest_strava_activity, ingest_strava_activities,
--    delete_strava_activity, log_manual_activity,
--    last_strava_sync_run, recent_strava_sync_runs
--
--  Admin RPCs
--    admin_list_challenge_participants, admin_challenge_participant_stats,
--    admin_booking_stats
--
--  Coupons / newsletter
--    validate_coupon, increment_coupon_usage, subscribe_to_newsletter
--
--  Triggers (function bodies referenced by triggers.sql)
--    activity_logs_sync_registration_total, registrations_assign_bib,
--    registrations_assign_certificate, notify_challenge_completed,
--    notify_milestone_unlocked, clubs_seed_owner_member,
--    club_members_block_last_owner_delete, bump_club_member_count,
--    recompute_club_member_count, orders_assign_booking_number,
--    orders_block_delete, payment_gateways_stamp_enabled,
--    payment_gateways_block_active_delete, guard_activity_log_registration,
--    guard_non_negative_distance, guard_registration_status_transition,
--    clubs_enforce_pending_for_users, update_updated_at_column
--
--  IMPORTANT — Supabase compatibility rewrites applied by the dumper:
--    * Every reference to `auth.uid()` remains unchanged; the compatibility
--      layer in Phase 5 sets `SET LOCAL auth.uid = '<uuid>'` per request and
--      installs a shim function `auth.uid() → current_setting('auth.uid')`.
--      That shim ships in Phase 4 via the auth module.
--    * `SECURITY DEFINER` clauses are preserved — the migration runner runs
--      as the `postgres` superuser on Railway, so ownership is correct.
--
--  DO NOT hand-edit — regenerate with `npm run schema:dump`.
-- =============================================================================
